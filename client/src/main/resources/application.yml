server:
  port: 8798
spring:
  application:
    name: service-hi

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    heartbeat-executor-exponential-back-off-bound: